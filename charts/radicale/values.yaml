# Default values for radicale.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: docker.io/tomsquest/docker-radicale
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

# This is for the secrets for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 5232

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /
    port: http
readinessProbe:
  httpGet:
    path: /
    port: http

# Additional volumes on the output Deployment definition.
volumes: []
# - name: foo
#   secret:
#     secretName: mysecret
#     optional: false

# Additional volumeMounts on the output Deployment definition.
volumeMounts: []
# - name: foo
#   mountPath: "/etc/foo"
#   readOnly: true

persistence:
  # This is to enable persistence more information can be found here: https://kubernetes.io/docs/concepts/storage/persistent-volumes/
  enabled: true
  # This is the size of the persistent volume more information can be found here: https://kubernetes.io/docs/concepts/storage/persistent-volumes/#access-modes
  size: 1Gi
  # This is the storage class for the persistent volume more information can be found here: https://kubernetes.io/docs/concepts/storage/storage-classes/
  storageClass: ""
  # This is for setting up annotations on the persistent volume more information can be found here: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
  annotations: {}
  # Set this to use an existing PVC instead of creating a new one
  existingClaim: ""

nodeSelector: {}

tolerations: []

affinity: {}

config:
  extFile:
    config:
  users:
    # user: password
    # user2: password2
  server:
    hosts:
    max_connections:
    max_content_length:
    timeout:
    ssl:
    certificate:
    key:
    certificate_authority:
    protocol:
    ciphersuite:
    script_name:
  encoding:
    request:
    stock:
  auth:
    type:
    cache_logins:
    cache_successful_logins_expiry:
    cache_failed_logins_expiry:
    ldap_ignore_attribute_create_modify_timestamp:
    ldap_uri:
    ldap_base:
    ldap_reader_dn:
    ldap_secret:
    ldap_secret_file:
    ldap_groups_attribute:
    ldap_filter:
    ldap_user_attribute:
    ldap_use_ssl:
    ldap_security:
    ldap_ssl_verify_mode:
    ldap_ssl_ca_file:
    dovecot_connection_type:
    dovecot_socket:
    dovecot_host:
    dovecot_port:
    imap_host:
    imap_security:
    oauth2_token_endpoint:
    pam_serivce:
    pam_group_membership:
    htpasswd_filename: /config/users
    htpasswd_encryption: bcrypt
    htpasswd_cache:
    delay:
    realm:
    lc_username:
    strip_domain:
  rights:
    type:
    file:
    permit_delete_collection:
    permit_overwrite_collection:
    urldecode_username:
  storage:
    type:
    filesystem_folder: /data/collections
    filesystem_cache_folder:
    use_cache_subfolder_for_item:
    use_cache_subfolder_for_history:
    use_cache_subfolder_for_synctoken:
    use_mtime_and_size_for_item_cache:
    folder_umask:
    max_sync_token_age:
    skip_broken_item:
    hook:
    predefined_collections:
  web:
    type:
  logging:
    level:
    mask_passwords:
    bad_put_request_content:
    backtrace_on_debug:
    request_header_on_debug:
    request_content_on_debug:
    response_content_on_debug:
    rights_rule_doesnt_match_on_debug:
    storage_cache_actions_on_debug:
  headers:
    Access-Control-Allow-Origin:
  hook:
    type:
    rabbitmq_endpoint:
    rabbitmq_topic:
    rabbitmq_queue_type:
  reporting:
    max_freebusy_occurrence:
